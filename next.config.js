/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static-export not used — we need server-side auth().
  // Vercel auto-detects Next.js; no extra config required.
};

module.exports = nextConfig;
